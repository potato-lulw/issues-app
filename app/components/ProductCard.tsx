'use client'
import React from 'react'

const ProductCard = () => {
  return (
    <div>
        <button onClick={()=>{console.log('hello')}}>Button</button>
    </div>
  )
}

export default ProductCard